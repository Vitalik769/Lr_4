import { MonsExceptionFilter } from './mon-exception.filter';

describe('MonsExceptionFilter', () => {
  it('should be defined', () => {
    expect(new MonsExceptionFilter()).toBeDefined();
  });
});
